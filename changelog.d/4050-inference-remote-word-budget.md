### Docs: the remote-inference page keeps its contract, its PR narrative goes

`docs/inference/remote.md` was 1,775 words, 275 of them a retelling of the
measurements that justified each contract - the websockets 16.x teardown timing,
a `host=` parse walked through to port 80, an advertised `0` traced through the
chunk-emitting check. Every rule, refused input and quoted refusal stays, the
page is 1,472 words, and it is graded by the 1,500-word budget rather than named
as owing a cut.

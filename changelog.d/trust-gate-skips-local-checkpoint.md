### Fixed
- `create_policy("lerobot_local", pretrained_name_or_path=<local dir>)` no longer demands `STRANDS_TRUST_REMOTE_CODE=1` for an ACT / Diffusion / TD-MPC / VQ-BeT checkpoint directory on disk - loading one executes no Hub code. Hub repo ids stay gated, and so do local checkpoints of every other type: in lerobot 0.5.x `policies/{evo1,gaussian_actor,groot,multi_task_dit,pi0,pi0_fast,pi05,smolvla,wall_x,xvla}` (directly or via `processor/tokenizer_processor.py`, whose `trust_remote_code` defaults to `True`) fetch tokenizers or encoders from the Hub with `trust_remote_code=True` even when the checkpoint itself is local. Do not widen the allow-list without re-running that grep.

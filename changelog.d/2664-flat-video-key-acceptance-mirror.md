### Quality

- **simulation/mujoco**: the example scanner's acceptance mirror now tracks the flat video keys `_dispatch_action` actually folds, instead of restating a rule that disagreed with the router for three of the four folding actions. `output_path`, `fps` and `camera_name` are folded into `video=` for `run_policy`, `start_policy`, `eval_policy` and `evaluate_benchmark`, but the fold cannot always honour one - a bare `camera_name` has no path to attach itself to, and a flat key sent with an explicit `video=` is not folded at all - and only `run_policy` accepts what is left. Measured against the live router over four actions and six payload shapes, the mirror disagreed in 9 of 24 cells, so an example naming the video camera for an eval rollout passed the guard and no-opped at runtime with `Unknown parameter 'camera_name'`. The fold is extracted into one owner (`_fold_flat_video_keys`) that the mirror runs on a probe payload rather than re-deriving; the residue `run_policy` alone accepts is named so the asymmetry is visible. The extraction is behaviour-identical - the accept/refuse matrix and `run_policy`'s video-wins-over-`output_path` outcome are byte-for-byte unchanged - and the file goes from 11 to 48 tests, adding a router-as-oracle check over every folding action, a planted-example regression, and a drift test tying its restated action set to the engine's own. No library behaviour changes.

### Docs: the ProtoMotions page keeps the tracker, the qpos bridge gets its own

`docs/policies/protomotions.md` was 1,709 words carrying two subjects, so the H2
boundary alone brings both inside the budget with no prose rewritten. The page is
1,244 words and keeps the tracker: the install line, the four-input observation
contract with the anchor link it declares, per-episode reset, the
`ProtoMotionsConfig` domains and the session stub. Turning a MuJoCo `qpos`
sequence into the cache it plays - the six channels, the frame-count rule, the
33-body reference MJCF read by row index and the file that loads without a Git
LFS mesh tree, the added floor, the world-frame velocity convention and the
restricted unpickler - is a new `docs/policies/protomotions-motion-cache.md` at
608 words.

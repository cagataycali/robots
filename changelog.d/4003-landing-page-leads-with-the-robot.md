### Docs: the landing page's first screen shows the robot, not the loop diagram

`docs/index.md` opened on `assets/hero_loop.svg`, a control-loop drawing, and its
three next-step cards pointed into `simulation/overview.md` (1,386 words) and
`hardware/robot-control.md` (1,267 words). The page now leads with the SO-101
pick that lifts, names the three readers it is written for, and hands off to the
Quickstart, the robot catalog and the agents page. `hero_loop.svg` moves to
`docs/agents.md`, the page whose subject it is. Two pins grade the first screen:
the leading image is a recording of a robot rather than a drawing, and every card
target is a page a newcomer finishes in one sitting.

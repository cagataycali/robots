### Docs: the domain-randomization page states its rules, not the runs behind them

`docs/simulation/domain-randomization.md` was the last page the 1,500-word
budget named as owing a cut, and every one of its sections is contract, so what
went is the measurements that justified a rule stated beside them: what
`"false"`, `"no"`, `"off"` and `"0"` each did before an axis flag was checked as
a boolean, a light traced from 3.5 m to 4.7 m over 50 episodes at 9.4x its
advertised bound, and a second copy of the fixed-reference derivation that the
`randomize_positions` paragraph above it already states. Both refusal rules, all
four refused spellings, the +/-0.5 m lighting bound, the seed-reproducibility
contract and every quoted refusal stay. The page is 1,426 words rather than
1,520, and it is graded by the budget rather than named as owing a cut.

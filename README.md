# allow_insecure setting domain

`allow_insecure_domain.png` is generated by `capture.py` (run once inside a checkout of
`strands-labs/robots` main and once inside the change) and `compose.py`, which asserts every
number it renders against `main.json` / `branch.json` before saving.

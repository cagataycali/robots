# set_eval_seed(None) — measured evidence

`measure_1949.py` was run once in a `git worktree` at `upstream/main` and once on
the branch; each dump records the tree it resolved so the two halves cannot be
the same code. `figure_1949.py` asserts every number it prints against those
dumps before saving.

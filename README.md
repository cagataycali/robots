Artifact for strands-labs/robots: "refuse a translation_scale or ik_smoothing the
IK path cannot honor".

vera_ik_domains.png -- composed figure.
renders/ -- the raw MuJoCo renders (headless EGL, 560x620) and the measured facts
each panel and table cell is derived from. `facts_main.json` is the upstream/main
tree, `facts_branch.json` the branch; the honored run is identical in both.

# PR #85 Demo Artifacts

Auto-generated visual artifacts for https://github.com/strands-labs/robots/pull/85
(MuJoCo simulation backend).

All frames produced by running `strands_robots.Simulation` with scripted
scenarios - no LLM calls, fully deterministic, regenerable via
`/tmp/pr85_artifacts/scripts/generate_demos.py` in the same checkout.

See the PR body for links to specific artifacts.
